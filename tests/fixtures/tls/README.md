These public, deliberately shared test-only certificates and private keys are used only by loopback TLS tests. They grant no access to any device or external service and are never included in the device trust bundle. The server certificate names raw.githubusercontent.com to test the enforced hostname against a local server; it is signed by an isolated test CA. Do not use these keys for any deployment.
